# 셸 스크립트는 Windows 에서 편집해도 항상 LF 로 저장한다.
# CRLF 가 섞이면 Mac/Linux 에서 "bad interpreter" 로 실행이 실패한다.
*.sh text eol=lf
*.yml text eol=lf
*.yaml text eol=lf

# Swift/문서는 체크아웃 시 플랫폼 기본을 따른다.
*.swift text
*.md text
